{
  "name": "cu-mealplan-bot",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "gen": "wrangler types && node src/scripts/gen-handlers.mjs",
    "deploy": "npm run gen && wrangler deploy"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "wrangler": "^4.45.3"
  },
  "dependencies": {
    "date-fns": "^4.1.0",
    "date-fns-tz": "^3.2.0"
  }
}
